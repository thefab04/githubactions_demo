name: actions

on:
  push:
    branches:
      - main
      - develop
      - release/**'

  pull_request:
    branches:
      - main
      

jobs:
  action_demo:
    runs-on: ubuntu-latest
    steps:
      - name: action-checkout
        uses: actions/checkout@v6
      - name: Print index.html file
        run: cat index.html

