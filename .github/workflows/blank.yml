name: actions

on: push

jobs:
  action_demo:
    runs-on: ubuntu-latest
    steps:
      - name: action-checkout
        uses: actions/checkout@v6
      - name: Print index.html file
        run: cat index.html

