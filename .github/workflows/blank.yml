name: actions

on: push

jobs:
  action_demo:
    runs-on: ubuntu-latest
    steps:
      - name: action-checkout
        uses: action/checkout@v4
      - name: Print index.html file
        run: cat index.html

